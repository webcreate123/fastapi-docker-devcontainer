services:
  todo-api:
    build: .
    ports:
      - "8000:8000"
      - "5678:5678"  # Add debug port
    volumes:
    - ..:/workspace:cached
    environment:
      - PYTHONPATH=/workspace
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
